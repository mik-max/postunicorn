import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Resume',
  description: 'Professional experience, education, and credentials of Israel Raphael.',
};

export default function ResumePage() {
  return (
    <main className="min-h-screen pt-24">
      {/* Sections: Experience timeline, Education, Skills, Download CTA */}
    </main>
  );
}
